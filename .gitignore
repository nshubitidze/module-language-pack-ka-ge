# Secrets / credentials — never commit
.env
.env.*
*.token
secrets/

# Build / runtime artifacts
/vendor/
/var/
/generated/
/composer.lock

# IDE / OS
/.idea/
/.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Regeneratable dev artifacts (not shipped; contributors can recreate via `python3 _translate.py`)
# _phrases-raw.csv is ~1.2 MB and comes from `bin/magento i18n:collect-phrases --magento`
_phrases-raw.csv
